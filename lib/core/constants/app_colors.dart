import 'package:flutter/material.dart';

class AppColors {
  // ===== PRIMARY BRAND =====
  static const Color primary = Color(0xFFD6A44A);       // Mostaza pastel
  static const Color primaryDark = Color(0xFFB5883C);   // Mostaza oscuro
  static const Color primaryLight = Color(0xFFFFF5D9);  // Mostaza muy suave

  // ===== ACCENT =====
  static const Color accent = Color(0xFFF3C978);        // Acento cálido
  static const Color accentSoft = Color(0xFFFFF3E0);    // Pastel suave

  // ===== BACKGROUND / SURFACES =====
  static const Color background = Color(0xFFFAF7F2);     // Beige ultraligero
  static const Color surface = Color(0xFFFFFFFF);         // Blanco puro
  static const Color surfaceVariant = Color(0xFFF2EDE6);  // Beige suave
  static const Color fieldBackground = Color(0xFFF7F3EB); // Campos modernos
  static const Color backgroundDisabled = Color(0xFFEAE5DF);

  // ===== TEXT =====
  static const Color textPrimary = Color(0xFF2E2A22);     // Carbón cálido
  static const Color textSecondary = Color(0xFF6C665A);   // Gris cálido
  static const Color textHint = Color(0xFFA9A499);

  // ===== ICONS =====
  static const Color iconPrimary = Color(0xFF2E2A22);
  static const Color iconSecondary = Color(0xFF6C665A);

  // ===== STATES =====
  static const Color success = Color(0xFF7BBF6A);        // Verde suave
  static const Color warning = Color(0xFFE7B64C);        // Mostaza brillante
  static const Color error = Color(0xFFCE5A57);          // Rojo quemado premium

  static const Color online = Color(0xFF76B56C);
  static const Color offline = Color(0xFFB4AAA0);
  static const Color blocked = Color(0xFFD9534F);

  // ===== BORDERS / SEPARATORS =====
  static const Color border = Color(0xFFD8D2C8);
  static const Color borderFocus = primary;

  // ===== EXTRAS =====
  static const Color shadow = Color(0x14000000); // Sombra suave
}
